import React, { useState } from "react";

const Application = () => {
  const [filter, setFilter] = useState([]);
  return (
    <div>
      <div>
        <header></header>
        <main></main>
      </div>
    </div>
  );
};

export default Application;
